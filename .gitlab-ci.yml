image: google/dart

before_script:
  - export PATH="$PATH":"~/.pub-cache/bin"
  - pub global activate grinder
  - pub get

test:
  stage: test
  script:
  - grind test

publish:
  stage: deploy
  script:
  - grind ci-publish
  only:
  - master@exitlive/gitlab-dart