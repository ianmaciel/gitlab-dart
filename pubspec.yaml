name: gitlab
# Increasing this version number *automatically* publishes this package
# when merged into master.
version: 0.4.1
description: A library to communicate with the GitLab API
homepage: https://gitlab.com/exitlive/gitlab-dart
documentation: https://pub.dartlang.org/documentation/gitlab/latest/
author: Matias Meno <m@tias.me>
environment:
  sdk: ">=1.21.0 <3.0.0"
dependencies:
  http: ^0.11.2
  logging: ^0.11.0
  intl: ^0.15.6
  meta: ^1.1.6
dev_dependencies:
  test: ^1.3.0
  path: ^1.6.0
  mockito: ^3.0.0
  grinder: ^0.8.1
  grind_publish: ^0.0.4
